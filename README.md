# meme-generator-react
A simple React meme generator built with Vite. Users can enter top and bottom text, fetch a random meme image from an API, and generate a custom meme. This project demonstrates React fundamentals including state management and side effects using the useEffect hook.
